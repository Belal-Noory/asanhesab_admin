import React from "react";

export const AppFooter = (props) => {
    return (
        <div className={"layout-footer "+props.hide}>
            <span className="font-medium ml-2">آسان حساب</span>
        </div>
    );
};
